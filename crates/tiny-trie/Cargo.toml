[package]
name = "tiny-trie"
version = "0.1.0"
edition = "2024"
description = "Compact arena-based radix tries (2/4/16-way) with SIMD lookup"
license = "MIT"

[dependencies]
benchable_map = { path = "../benchable_map" }
serde = { version = "1.0.228", features = ["derive"] }

[features]

[package]
name = "tiny-trie"
version = "0.1.0"
edition = "2024"
description = "Compact arena-based radix tries (2/4/16-way) with SIMD lookup"
license = "MIT"

[dependencies]
tiny-trie-trait = { path = "../tiny-trie-trait" }
serde = { version = "1.0.228", features = ["derive"] }

[features]

I think i want a routine start+end session routine. 
I want to make a commit before each session writes any code, and have it run a process at the end of each session.

I think the claude.toml's purpose should be to get the agent familiar with the concepts involved in the codebase. The code itself shouldnt have paragraph comments, but rather inline and header comments with different purposes. 

Between the two levels, an outline generator is necessary. Instead of writing one in advance and saving it in claude.toml, and meticulously updating it, the outline should be procedurally generated. From the files, the things defined therein, and their 'header comments'.

What belongs in a header comment? 

Also i want something regarding code quality but im not sure what to say to prioritize/do. 

I also want the conversation responses to have a particular style - summary first, details after inquiry. 